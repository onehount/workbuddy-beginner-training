# 安全与隐私说明

本项目是**公开教学仓库**，不承载任何生产系统。本文档说明本项目的隐私边界、脱敏原则，以及发现问题时的处理方式。

---

## 一、本仓库的隐私承诺

本仓库承诺**不包含**以下任何内容：

| 类别 | 说明 |
|---|---|
| 真实企业信息 | 公司名称、Logo、商标、组织架构、内部制度 |
| 真实人员信息 | 姓名、工号、手机号、邮箱、身份证号、住址 |
| 凭据类信息 | 账号、密码、Token、API Key、Cookie、Session |
| 网络信息 | 内网 IP、内网域名、堡垒机地址、数据库连接串 |
| 真实业务数据 | 真实营收、真实客户、真实渠道数据、真实合同 |
| 环境痕迹 | 真实用户目录路径、真实历史任务、真实聊天窗口 |

**Demo 案例中的一切均为虚构。** 品牌「青屿茶研 QINGYU TEA LAB」、6 名参会人员、5 项市场活动、全部渠道数据均为教学构造。

---

## 二、脱敏检查项（每次发布前必过）

### 2.1 文本类

- [ ] 全文检索真实公司名，结果为 0
- [ ] 全文检索 `@` 邮箱格式，仅剩虚构域名
- [ ] 全文检索 11 位手机号格式，结果为 0
- [ ] 全文检索 `C:\Users\`、`/home/`、`/Users/` 真实用户名，结果为 0
- [ ] 全文检索 `token`、`secret`、`password`、`api_key`，无真实值

### 2.2 截图类

- [ ] 窗口标题栏无真实文件路径
- [ ] 侧边栏无真实历史任务名称
- [ ] 无系统通知弹窗、无聊天软件窗口
- [ ] 无浏览器书签栏、无浏览器标签页标题
- [ ] 无任务栏中可识别的第三方软件图标组合
- [ ] 无真实头像、无真实账号昵称

### 2.3 文件属性类

- [ ] 所有 Office 文件的「作者」统一为 `WorkBuddy Training Project`
- [ ] 所有 Office 文件的「上次保存者」已清空或统一
- [ ] 已删除历史版本信息与修订记录
- [ ] 已删除文档批注中的真实人名
- [ ] PPT 中无残留的备注页真实信息

自动检查：

```bash
python tools/check_privacy.py
```

该脚本会扫描仓库内所有 `.md`、`.docx`、`.xlsx`、`.pptx`、`.png`，输出可疑命中清单。**脚本是辅助，不能替代人工复核。**

---

## 三、如果你发现了隐私问题

如果你在本仓库中发现了**疑似真实的**企业信息、个人信息或凭据：

### 请这样做 ✅

1. **不要**公开开 Issue（那会扩大暴露面）
2. 通过 GitHub 的 **Private vulnerability reporting** 提交
   （仓库页 → Security → Report a vulnerability）
3. 说明：文件路径 + 大致位置 + 你判断为真实信息的理由

### 我们会这样做

| 时间 | 动作 |
|---|---|
| 48 小时内 | 确认收到并初步判断 |
| 确认属实后 7 天内 | 移除内容、重写受影响文件、发布修订版本 |
| 必要时 | 重写 Git 历史（`git filter-repo`）并强制推送 |

---

## 四、使用本材料时的注意事项

如果你把这套材料改造成**你自己公司的版本**：

> ⚠️ **改造后的版本默认是内部材料，不要直接公开。**

必须先完成：

1. 替换全部虚构数据为你的数据后，**重新做一遍第二节的全部检查项**
2. 确认你的业务数据**允许对外披露**（多数情况下不允许）
3. 确认截图中不含内部系统界面、内部项目名、内部人员信息
4. 走一遍你公司的对外发布审批流程

**课程中反复强调的一条学员纪律，同样适用于你：**

> 上传给 AI 之前，先问自己一句 —— 这份材料如果出现在公开网页上，我能接受吗？

---

## 五、不适合上传给 WorkBuddy 的材料（教学内容摘录）

本课程第八章会明确告诉学员，以下材料**不要上传**：

- 未脱敏的客户名单、供应商合同
- 含身份证号、银行卡号、手机号的表格
- 标记为「机密」「内部」「限阅」的文件
- 未公开的财务数据、并购信息、人事任免
- 他人的个人隐私信息
- 你没有处置权限的文件

**判断原则：不确定，就先问，不要先传。**

---

## 六、依赖与工具安全

`tools/` 下的脚本仅依赖以下开源库，均无网络请求行为：

| 依赖 | 用途 | 许可证 |
|---|---|---|
| Pillow | 截图裁剪、放大、合成 | HPND |
| python-docx | 文档生成与检查 | MIT |
| openpyxl | 表格生成与检查 | MIT |
| python-pptx | 演示文稿生成与检查 | MIT |

脚本**不会**：上传任何文件、访问任何网络地址、读取仓库以外的目录、修改系统配置。

---

_最后更新：2026-08-09_
